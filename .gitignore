# ============================================================================
# 1. VISUAL STUDIO & IDE GENERATED FILES (File sinh ra bởi IDE)
# ============================================================================
# Thư mục cấu hình ẩn của Visual Studio (Chứa setting người dùng, breakpoint...)
.vs/

# File cấu hình người dùng (User-specific options)
*.user
*.userosscache
*.sln.docstates

# File cơ sở dữ liệu intellisense (Code completion database)
*.db
*.opendb
*.vc.db

# File tạm của Solution (Solution User Options)
*.suo

# ============================================================================
# 2. BUILD OUTPUT & INTERMEDIATE FILES (Sản phẩm biên dịch & File trung gian)
# ============================================================================
# Các thư mục chứa file binary sau khi build (Debug/Release)
# Cấu trúc thường thấy: x64/Debug, x64/Release
x64/
x86/
Debug/
Release/
build/
bin/
obj/

# File thực thi (Executables) - Chúng ta build lại từ code, không commit file exe
*.exe
*.out
*.app

# Thư viện liên kết động/tĩnh (Dynamic/Static Libraries) tạo ra khi build
# Lưu ý: Nếu bạn có folder "Libraries" chứa dll bên thứ 3 cần commit, hãy dùng dấu ! để ngoại lệ
*.dll
*.lib
*.so
*.dylib

# File Debug Information (Thông tin gỡ lỗi)
*.pdb
*.idb
*.ilk

# File Object (Kết quả biên dịch từng file .cpp)
*.obj
*.o

# File lưu trữ biên dịch sẵn (Precompiled Headers)
*.pch
*.ipch

# ============================================================================
# 3. SYSTEM & OS FILES (File rác của Hệ điều hành)
# ============================================================================
# Windows thumbnail cache
Thumbs.db
ehthumbs.db
Desktop.ini

# MacOS directory attributes
.DS_Store
.AppleDouble
.LSOverride

# ============================================================================
# 4. VISUAL STUDIO CODE (Nếu có thành viên dùng VS Code)
# ============================================================================
.vscode/

# ============================================================================
# 5. LOGS & TEMPORARY FILES (File nhật ký & Tạm)
# ============================================================================
*.log
*.tlog
*.tmp
*.bak
*.swp

# ============================================================================
# 6. ASSET INTERMEDIATES (File tạm của phần mềm đồ họa - Tùy chọn)
# ============================================================================
# Photoshop backup
*.psdtmp
# Blender backup
*.blend1
*.blend2

# ============================================================================
# 7. EXCEPTIONS (NGOẠI LỆ - BẮT BUỘC PHẢI GIỮ LẠI)
# ============================================================================
# Đảm bảo Git không bao giờ ignore file mã nguồn và tài nguyên game
!src/
!include/
!assets/
!CMakeLists.txt
!.gitignore
!.gitattributes